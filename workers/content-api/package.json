{
  "name": "clash-content-api-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "echo \"Use project-level tooling for local worker dev\"",
    "typecheck": "../../node_modules/.bin/tsc --noEmit -p tsconfig.json",
    "build": "../../node_modules/.bin/tsc --noEmit -p tsconfig.json",
    "check": "npm run typecheck && npm run build",
    "deploy": "echo \"Deploy is intentionally disabled in CI\""
  },
  "devDependencies": {}
}
